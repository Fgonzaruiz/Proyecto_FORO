## Entry points HTML (`/game/*`)

- `index.php`: landing mínima de `/game/`

Convención:
- Cada página pública vive aquí (HTML a través de templates MyBB).
- La lógica de aplicación vive fuera (`game/src/*`).

